These are the two versions — Android and Windows.

Windows version:
Simply open the .exe file once. It will automatically register itself in Windows startup, so you won't need to open it again manually — it will launch on every boot.

After that, just keep an eye on the same folder where the .exe is located. A file called found.txt will appear there whenever a funded key is found.

Good luck.
